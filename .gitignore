# Environment files — never commit real or template secrets
.env
.env.*

# Internal opencode tooling (agent configs + skills) — team-internal, not shipped
.opencode/
# Downloaded Spark/Airflow jars (build cache; large binaries, not committed)
jars/

# Python bytecode
__pycache__/
